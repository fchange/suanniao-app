Place the local voice file here as `suanniao.mp3`.

Expected content: one short Chinese voice line saying "算鸟算鸟".

You can also replace it with `suanniao.m4a`. The app will try `mp3` first, then `m4a`.
